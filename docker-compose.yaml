services:
  app:
    image: devops-sharing:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    networks:
      - devops-network

networks:
  devops-network:
    driver: bridge
